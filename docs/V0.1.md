# Graphcraft v0.1 implementation report

Status: runnable local alpha
Date: July 21, 2026

## Acceptance boundary

Version 0.1 is the first executable Graphcraft slice. It is accepted as an alpha when it can:

1. compile and display a user-owned run contract;
2. persist an execution graph and governance anchors;
3. isolate writes in a new Git worktree;
4. invoke either supported host through structured, non-interactive output;
5. verify work with deterministic repository probes;
6. checkpoint every transition in a hashed event log;
7. rebuild corrupted state and recover interrupted running or waiting nodes without repeating accepted nodes;
8. distinguish measurable progress from unchanged work and stop safely;
9. expose the same lifecycle through a CLI and a small MCP surface; and
10. stay below the planned 250-token plugin discovery limit.

This boundary deliberately excludes remote GitHub mutations and stable-release performance claims.

## Implemented architecture

The pnpm workspace separates:

- `core`: schemas, graph compilation and validation, context capsules, event hashing and replay, progress leases, and host interfaces;
- `runtime`: event storage, atomic materialized views, locks, worktrees, deterministic waits, detached supervision, scheduling, recovery, general graph amendments, and run lifecycle;
- `probes`: shell-free command execution, file checks, Git evidence, repository test discovery, and workspace hashing;
- `adapter-codex` and `adapter-claude`: capability and authentication checks, structured worker execution, normalized events, and token usage;
- `github`: bounded `gh` capability checks and read-only, fully paginated, SHA-bound pull-request snapshots;
- `cli`: installation, contracts, lifecycle controls, and human or JSON rendering; and
- `mcp`: one low-discovery-cost tool covering run, status, inspect, probes, durable control decisions, resume, pause, stop, trace, and doctor actions.

The runtime also exposes an experimental matched benchmark harness. It is evaluation infrastructure, not evidence that the stable efficiency target has been met.

The event log is the source of truth. `state.json` and `graph.json` are rebuildable views. Workers can change repository files but cannot edit the approved contract or governance anchors.

## Verified behavior

Automated coverage includes:

- strict contract and worker schemas plus JSON Schema export, including Codex-compatible strict-output normalization with independent runtime validation;
- schema-constrained Codex and Claude graph planning with deterministic contract, scope, dependency, side-effect, path, and probe validation;
- grounded fallback and planned-path context selection, bounded model-derived summaries, content-addressed capsule/inventory reuse, and per-invocation selected/omitted/reused context receipts;
- graph dependency and cycle validation;
- versioned bug, feature, migration, refactor, and audit probe plans with deterministic repository inventories, declared focused/acceptance checks, bounded validation, and pre-approval editing;
- event-authoritative held-out completion plans with opaque graph references, runtime-only resolution, package-script and direct measurement-file integrity checks, sanitized repair evidence, and tamper-safe materialized recovery;
- isolated read-only semantic verification for structurally ambiguous progress or completion, with strict verdicts, mutation detection, separate artifacts, and token receipts;
- runtime enforcement for observing, veto, arbitration, and target-ownership edges, including sticky user decisions, explicit replacement, evidence-backed overrides, and durable conflict packets;
- evidence-backed add, supersede, split, fuse, and dependency amendments with immutable accepted work, contract-policy revalidation, revision history, explicit authority expansion, and repeated-strategy stopping;
- deterministic ready-node batching with default-sequential execution, opt-in two-worker read-only fan-out, serialized event appends, sibling quarantine, and multi-invocation resume;
- event hash verification and deterministic replay;
- a strict run-storage manifest covering every durable path class, forward-only and idempotent pre-manifest migration, complete pre-migration backups, precise future-format refusal, and current inspection/resume of runs produced by both signed 0.1 release tags;
- persisted task-specific progress vectors and strategy trajectories, including migration inventory and failing-probe dimensions, restart-safe A→B→done versus A→B→A classification, unchanged-signature strategy enforcement, and concise durable decision packets;
- provider-aware Codex and Claude token normalization, explicit per-dimension availability, event-authoritative planning/worker/repair/semantic/overhead attribution, recovered-receipt reconciliation, and whole-run, per-phase, and per-node cost reports;
- deterministic pre-approval graph fusion and safe broad-write splitting, evidence-backed serial/parallel scheduling, cost-grounded exact-session reuse versus fresh contexts, typed optimizer trace decisions, and task-shape small-work bypass without word-count heuristics;
- a versioned ten-task bug, feature, migration, refactor, audit, and PR-repair benchmark corpus; deterministic fresh Git fixtures; randomized baseline/Graphcraft schedules; explicit dual-host model and effort policy propagation; matched executable and structural scorers; atomic partial reports; trial-ID resume; and gates that reject incomplete, unreconciled, or control-mismatched comparisons;
- progress, learning, completion, regression, oscillation, and stall classification;
- real temporary Git repositories and isolated worktrees;
- a successful local run with token accounting;
- a failed deterministic check followed by a graph amendment and successful repair;
- a no-progress run that stops as stalled;
- materialized-state corruption rebuilt from events;
- exclusive run locking;
- durable host-session, transcript, and progress-baseline checkpoints with completed-result reuse, native-session continuation, and fault-injected recovery across every invocation boundary for both host identities;
- coordinated cross-process pause/stop with bounded child termination, typed outcomes, and post-reconciliation lock release;
- event-authoritative time, file-exists, and content-based file-changed waits with persisted baselines, observations, timeouts, and next-wake conditions; unchanged waits consume no model tokens and restart after satisfaction does not duplicate acceptance;
- detached local supervision with atomic versioned PID/heartbeat records, mode-`0600` logs, duplicate-owner rejection, stale-process replacement, status/inspect projection, and process-level restart-to-completion coverage in a repository path containing spaces;
- read-only GitHub capability reports and pull-request snapshots covering repository permission, base-branch protection, exact head/base SHAs, required checks, reviews, review threads, mergeability, and rate limits; pagination and final SHA revalidation reject stale mixed-head evidence;
- an event-authoritative claim–act–confirm journal for atomic commits, with immutable preconditions, deterministic idempotency keys, commit trailers, restart reconciliation, uncertain-outcome refusal, and fault injection across staging, action, and confirmation boundaries;
- versioned MCP-runtime staging outside ephemeral package-manager caches; and
- an allowlisted npm tarball containing only the two executables and required package documents.

A real-host dogfood run used the bundled CLI against a disposable repository. The initial Codex child was unavailable because that shell CLI was not authenticated; Graphcraft stopped without claiming progress. The same persisted run resumed with authenticated Claude Code. The worker made the requested implementation and tests, the parent process was interrupted, and a second resume recovered the stale lock and existing worktree. Independent `npm test` verification passed three tests, including the requested empty-input behavior, and the run reached `completed`.

The bundled MCP server was also exercised through a real TypeScript MCP client: initialization, tool discovery, and a structured `doctor` call all succeeded. Both Codex and Claude plugin manifests pass their native validators.

Current live adapter evaluation also covers Codex CLI 0.144.6: bug, feature, migration, refactor, and audit prompts produced five distinct valid repository-grounded graphs; an exact session resumed after the first process was terminated; and isolated semantic verification returned grounded evidence without changing its fixture. This machine requires a temporary `devx codex` launcher for local credentials, but that launcher is not part of Graphcraft or its public command behavior.

The public `@tpypan/graphcraft@0.1.1` package was installed into a clean global prefix and executed successfully. Both `npm exec --package @tpypan/graphcraft` and `npx @tpypan/graphcraft` resolved the public package and reported the expected version from an empty directory.

## Token posture

The Codex plugin discovery metadata is checked in CI and currently estimates below 250 tokens. Worker calls receive a content-addressed context capsule rather than prior transcripts or raw logs. Polling, Git inspection, hashing, graph traversal, state replay, and status output are deterministic and do not require model calls.

The real-host dogfood produced token receipts. Graphcraft now preserves provider limitations explicitly and reports normalized whole-run, phase, and node costs. Its benchmark harness can gather matched receipts, but no complete repeated dual-host result or blinded defect review has been accepted. Version 0.1 therefore makes no claim that it has reached the stable plan's 20% median savings target.

## Known limitations

- Only `local_verified` and `committed` finish lines execute. Remote finish-line requests fail clearly instead of being silently narrowed.
- Codex and Claude CLI authentication is separate from the desktop app session and is checked before execution.
- Local probe planning covers bug, feature, migration, refactor, and audit tasks using repository inventories, relevant declared package scripts, and broader Node, Python, and Go checks. PR-lifecycle probes are not yet integrated with the new SHA-bound GitHub snapshot.
- The GitHub layer is inspection-only. Push, pull-request mutation, CI/review automation, check rerun, review reply/resolution, merge, and deployment remain absent and cannot be inferred from a snapshot. Telemetry and the graphical trace viewer are also absent. Benchmark infrastructure exists, but repeated dual-host trials, blinded review, published results, and the stable efficiency gate remain incomplete.
- Wait nodes currently support local time and isolated-worktree filesystem conditions. Provider events and GitHub-aware backoff remain future capabilities. After a machine restart, the user restarts a persisted run with `graphcraft resume --background`; Graphcraft does not install an auto-start service.
- npm distribution is live, but official Codex and Claude marketplace publication still requires its external submission and review steps. Direct installation from the public GitHub repository remains the registry-independent fallback.
